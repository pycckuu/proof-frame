// ProofFrame shared types
// These types are used by guest (riscv32im), host (native), and frontend (via serde JSON).
// MUST NOT depend on risc0-zkvm — must compile for both targets.

// TODO: Implement T1 — see tasks/TASKS.md
